@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Generate two-factor authentication (2FA) recovery codes";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<h1 class="mb-4 text-2xl font-semibold text-gray-900 dark:text-gray-100">
    @ViewData["Title"]
</h1>

<div class="text-yellow-700 rounded-md bg-yellow-50 p-4 max-w-screen-md" role="alert">
    <p>
        <strong>This action generates new recovery codes.</strong>
    </p>
    <p class="my-2">
        If you lose your device and don't have the recovery codes you will lose access to your account.
    </p>
    <p class="my-2">
        Generating new recovery codes does not change the keys used in authenticator apps. If you wish to change the key
        used in an authenticator app you should 
        <a class="@Css.Link" asp-page="./ResetAuthenticator">reset your authenticator keys.</a>
    </p>
</div>

<div>
    <form method="post">
        <button class="@Css.DangerButton" type="submit">Generate Recovery Codes</button>
    </form>
</div>
